import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    padding: 4,
  },

  input: {
    padding: 2,
    borderRadius: 5,
    borderColor: "black",
    borderWidth: 1,
  },
});
